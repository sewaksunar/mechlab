"""
shared — generic, dependency-free helpers usable by ANY layer
(string formatting, small math utilities, etc). Nothing here should
import from domain/engine/application/interfaces — this package sits
"outside" the dependency direction, available to everyone.
"""
