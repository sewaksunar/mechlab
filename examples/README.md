# Examples

This folder contains small, runnable scripts that demonstrate MechLab usage.

- `plane_stress.py`: Plane stress calculations with `StressState`.
- `stress_transform.py`: 3D stress tensor transformation (symbolic + numeric).

Run from the repo root:

```bash
uv run python examples/plane_stress.py
uv run python examples/stress_transform.py
```
