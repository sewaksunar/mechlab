{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"mechlab","text":"<p>A modular, object-oriented engineering mechanics library for statics, dynamics, and mechanics-of-materials analysis.</p> <pre><code>from mechlab import BeamAnalysis\nfrom mechlab.domain.entities import Material, Section\n\nsteel = Material(name=\"Steel A36\", young_modulus=200e9, yield_strength=250e6)\nsection = Section(name=\"W150x18\", moment_of_inertia=9.19e-6, area=2.3e-3,\n                   extreme_fiber_distance=0.076)\n\nresult = (\n    BeamAnalysis(length=4.0, material=steel, section=section)\n    .set_simple_supports(0.0, 4.0)\n    .add_point_load(2.0, 5000)\n    .run()\n)\nprint(result[\"safety_factor\"])\n</code></pre>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Getting Started \u2014 install and run your first analysis</li> <li>Architecture \u2014 how the four layers fit together</li> <li>Project Guide \u2014 practical guide to structure, workflow, adding features, and git habits</li> <li>API Reference \u2014 auto-generated from docstrings</li> <li>CLI Reference \u2014 command-line usage</li> </ul>"},{"location":"architecture/","title":"Architecture","text":"<p>mechlab is organized into four layers with a strict one-directional dependency flow:</p> <pre><code>interfaces/   -&gt; CLI, reports, plots        (user/system facing)\napplication/  -&gt; workflow orchestration      (the public API)\nengine/       -&gt; solvers, units, math        (numerical machinery)\ndomain/       -&gt; Body, Beam, Load, Material  (pure engineering science)\n</code></pre> <p>A layer may only import from layers below it. <code>domain/</code> never imports from <code>engine/</code>; <code>engine/</code> never imports from <code>application/</code>. This keeps the physics/math (<code>domain/</code>) unit-testable in complete isolation, with no I/O, no mocks, no fixtures required.</p>"},{"location":"architecture/#design-patterns","title":"Design patterns","text":"<ul> <li>Abstract Base Class \u2014 <code>Body</code> and <code>Load</code> define contracts that   subclasses (<code>Beam</code>, <code>PointLoad</code>, <code>DistributedLoad</code>) must fulfill.</li> <li>Strategy \u2014 <code>EquilibriumSolver</code> is composed into <code>Beam</code> rather   than inherited, so the solving algorithm can be swapped without   touching the <code>Beam</code> class.</li> <li>Facade \u2014 <code>BeamAnalysis</code> hides domain/engine complexity behind a   small, fluent public API.</li> <li>Method chaining \u2014 <code>add_load()</code>, <code>add_support()</code>, etc. return   <code>self</code>, enabling a readable fluent interface.</li> </ul>"},{"location":"architecture/#extending-mechlab","title":"Extending mechlab","text":"<p>Adding a new <code>Body</code> type (e.g. <code>Truss</code>):</p> <ol> <li>Create <code>domain/statics/truss.py</code>, subclass <code>Body</code>, implement <code>solve()</code>.</li> <li>Reuse <code>EquilibriumSolver</code> from <code>engine/math/</code>, or add a new solver    class there \u2014 keep solving algorithms out of <code>domain/</code>.</li> <li>Add a facade method/class in <code>application/api.py</code>.</li> <li>Wire a CLI subcommand in <code>interfaces/cli/commands.py</code> if needed.</li> <li>Add tests under <code>tests/domain/</code>, <code>tests/engine/</code>, <code>tests/application/</code>.</li> </ol> <p>Adding a new unit:</p> <p>Call <code>UnitRegistry().register(\"symbol\", si_factor)</code>, or add it to the default table in <code>engine/units/registry.py</code>.</p>"},{"location":"cli/","title":"CLI Reference","text":""},{"location":"cli/#mechlab-beam","title":"<code>mechlab beam</code>","text":"<p>Analyze a simply-supported beam under point and distributed loads.</p> <pre><code>python -m mechlab beam \\\n    --length 4.0 --E 200e9 --yield 250e6 \\\n    --I 9.19e-6 --area 2.3e-3 --c 0.076 \\\n    --support 0.0 --support 4.0 \\\n    --point-load 2.0 5000\n</code></pre> Flag Required Description <code>--length</code> yes Beam length (m) <code>--E</code> yes Young's modulus (Pa) <code>--yield</code> yes Material yield strength (Pa) <code>--I</code> yes Moment of inertia (m\u2074) <code>--area</code> yes Cross-sectional area (m\u00b2) <code>--c</code> yes Extreme fiber distance (m) <code>--support</code> yes, \u00d72 Support position (m). Pass twice for the two supports. <code>--point-load</code> no, repeatable <code>POSITION MAGNITUDE</code> \u2014 pass multiple times for multiple loads"},{"location":"cli/#example-output","title":"Example output","text":"<pre><code>=== Beam Analysis Report ===\n\nReactions:\n  x = 0.000 m  -&gt;  R = 2500.00 N\n  x = 4.000 m  -&gt;  R = 2500.00 N\n\nMax bending stress : 41.35 MPa at x = 2.00 m\nSafety factor       : 6.05\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Get MechLab installed in just a few minutes and run your first structural analysis.</p>"},{"location":"getting-started/#installation","title":"Installation","text":"<p>Install the latest stable release from PyPI.</p> <p></p> pipuvDevelopment <pre><code>pip install mechlab\n</code></pre> <pre><code>uv pip install mechlab\n</code></pre> <p>Clone the repository and install the development dependencies.</p> <pre><code>uv sync --extra dev\n</code></pre> <p>Using uv?</p> <p><code>uv</code> is a fast Python package manager and drop-in replacement for many <code>pip</code> workflows.</p>"},{"location":"getting-started/#verify-the-installation","title":"Verify the Installation","text":"<p>After installation, verify that MechLab is available.</p> PythonCLI <pre><code>import mechlab\n\nprint(mechlab.__version__)\n</code></pre> <p>Output</p> <pre><code>0.1.0\n</code></pre> <pre><code>python -m mechlab --version\n</code></pre> <p>Output</p> <pre><code>mechlab 0.1.0\n</code></pre> <p>Success</p> <p>If a version number is displayed, MechLab has been installed successfully.</p>"},{"location":"getting-started/#your-first-analysis","title":"Your First Analysis","text":"<p>The example below creates a simply supported beam, applies both a point load and a uniformly distributed load, and runs the analysis.</p> <pre><code>from mechlab import BeamAnalysis\nfrom mechlab.domain.entities import Material, Section\n\nsteel = Material(\n    name=\"Steel A36\",\n    young_modulus=200e9,\n    yield_strength=250e6,\n)\n\nsection = Section(\n    name=\"W150x18\",\n    moment_of_inertia=9.19e-6,\n    area=2.3e-3,\n    extreme_fiber_distance=0.076,\n)\n\nanalysis = (\n    BeamAnalysis(length=4.0, material=steel, section=section)\n    .set_simple_supports(pos_a=0.0, pos_b=4.0)\n    .add_point_load(position=2.0, magnitude=5000)\n    .add_distributed_load(\n        start=0.0,\n        end=4.0,\n        intensity=1000,\n    )\n)\n\nreport = analysis.run()\n\nprint(report)\n</code></pre>"},{"location":"getting-started/#command-line-interface","title":"Command-Line Interface","text":"<p>You can also perform analyses directly from the command line.</p> <pre><code>uv run python -m mechlab beam \\\n  --length 4.0 \\\n  --E 200e9 \\\n  --yield 250e6 \\\n  --I 9.19e-6 \\\n  --area 2.3e-3 \\\n  --c 0.076 \\\n  --support 0.0 \\\n  --support 4.0 \\\n  --point-load 2.0 5000\n</code></pre> <p>Info</p> <p>See the CLI Reference for the complete list of commands and options.</p>"},{"location":"getting-started/#running-the-test-suite","title":"Running the Test Suite","text":"<p>If you're contributing to MechLab or working from source, run the test suite with:</p> <pre><code>uv run pytest\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<p>Now that MechLab is installed, you can:</p> <ul> <li>Learn the core API in the User Guide</li> <li>Explore the CLI Reference</li> <li>Browse practical examples and tutorials</li> <li>Start building structural analysis workflows with MechLab</li> </ul>"},{"location":"project-guide/","title":"Project Guide","text":"<p>This guide is for someone who understands mechanics and wants a practical way to work on the code without needing to be a full-time software developer.</p>"},{"location":"project-guide/#what-mechlab-is","title":"What MechLab Is","text":"<p>MechLab is a modular engineering mechanics library. The working example today is beam analysis, but the structure supports future topics: trusses, shafts, dynamics, and design checks.</p> <p>At a high level, the library takes physical inputs (material properties, geometry, supports, loads), solves the structure, computes internal responses, and formats results for the CLI, reports, plots, or Python scripts.</p>"},{"location":"project-guide/#project-structure","title":"Project Structure","text":"<p>The repository is organized by responsibility, not by feature size.</p> Source CodeProject Root <pre><code>src/mechlab/\n\u251c\u2500\u2500 domain/       \u2190 pure engineering objects and rules\n\u251c\u2500\u2500 engine/       \u2190 numerical methods and unit handling\n\u251c\u2500\u2500 application/  \u2190 user-facing workflows and public API\n\u251c\u2500\u2500 interfaces/   \u2190 CLI, reports, and plotting\n\u2514\u2500\u2500 shared/       \u2190 small helpers used across layers\n</code></pre> <pre><code>docs/              documentation source (MkDocs)\ntests/             tests grouped to match code layers\nexamples/          small runnable scripts\nREADME.md          quick project overview\nCONTRIBUTING.md    contributor workflow\npyproject.toml     package metadata and tooling config\n</code></pre>"},{"location":"project-guide/#architecture","title":"Architecture","text":"<p>The code follows a strict one-way dependency flow:</p> <pre><code>interfaces \u2192 application \u2192 engine \u2192 domain\n</code></pre> <p>Lower layers never import from higher layers.</p> Layer Responsibility Key modules <code>domain/</code> Engineering concepts: <code>Material</code>, <code>Section</code>, <code>Load</code>, <code>Support</code>, <code>Beam</code> <code>entities.py</code>, <code>strength/beam.py</code> <code>engine/</code> Math and solving logic <code>math/solvers.py</code> <code>application/</code> Public facade: <code>BeamAnalysis</code> <code>api.py</code>, <code>workflows.py</code> <code>interfaces/</code> Presentation: CLI, text reports, plots <code>cli/commands.py</code>, <code>output/report.py</code>, <code>visual/plots.py</code> <p>Why this matters</p> <p>This separation makes the core mechanics easy to test, easy to extend, and reusable across different interfaces.</p>"},{"location":"project-guide/#how-the-library-works","title":"How the Library Works","text":"<pre><code>graph LR\n    A[Create Material &amp; Section] --&gt; B[Build BeamAnalysis]\n    B --&gt; C[Add supports &amp; loads]\n    C --&gt; D[Call run]\n    D --&gt; E[Read results or format output]\n    E --&gt; F[Optionally plot diagrams]\n</code></pre> Key classes at a glance Class Purpose <code>mechlab.domain.entities.Material</code> Young's modulus, yield strength <code>mechlab.domain.entities.Section</code> Inertia, area, extreme fiber distance <code>mechlab.domain.entities.PointLoad</code> / <code>DistributedLoad</code> / <code>PointMoment</code> Load definitions <code>mechlab.domain.entities.Support</code> / <code>SupportType</code> Boundary conditions <code>mechlab.domain.strength.beam.Beam</code> The beam object itself <code>mechlab.application.api.BeamAnalysis</code> Public API facade <code>mechlab.engine.math.solvers.MatrixBeamSolver</code> Matrix-based beam solver <code>mechlab.interfaces.output.report.ReportGenerator</code> Text output <code>mechlab.interfaces.visual.plots</code> Shear, moment, FBD diagrams <p>Note</p> <p>The solver currently used by <code>Beam</code> is the matrix-based solver. A simpler two-support equilibrium solver also exists in <code>engine/math/solvers.py</code> as a reference.</p>"},{"location":"project-guide/#how-to-use-it","title":"How to Use It","text":""},{"location":"project-guide/#install-for-development","title":"Install for development","text":"<pre><code>uv sync --extra dev --extra docs\n</code></pre>"},{"location":"project-guide/#run-the-example","title":"Run the example","text":"<pre><code>PYTHONPATH=src python examples/simply_supported_beam.py\n</code></pre>"},{"location":"project-guide/#run-tests","title":"Run tests","text":"<pre><code>uv run pytest\n</code></pre>"},{"location":"project-guide/#live-preview-docs","title":"Live preview docs","text":"<pre><code>uv run mkdocs serve\n</code></pre> <p>Tip</p> <p><code>mkdocs serve</code> starts a local server with auto-reload. Use <code>mkdocs build</code> only when you need a deployable static site.</p>"},{"location":"project-guide/#lint-and-type-checks","title":"Lint and type checks","text":"<pre><code>uv run ruff check src tests examples\nuv run mypy\n</code></pre>"},{"location":"project-guide/#use-the-cli","title":"Use the CLI","text":"<pre><code>uv run python -m mechlab beam \\\n  --length 4.0 \\\n  --E 200e9 \\\n  --yield 250e6 \\\n  --I 9.19e-6 \\\n  --area 2.3e-3 \\\n  --c 0.076 \\\n  --support 0.0 \\\n  --support 4.0 \\\n  --point-load 2.0 5000\n</code></pre>"},{"location":"project-guide/#package-and-publish","title":"Package and publish","text":"<pre><code>uv build      # creates sdist + wheel locally\nuv publish    # uploads to PyPI\n</code></pre> <p>PyPI authentication</p> <p>PyPI no longer accepts account passwords.</p> <ul> <li>Use <code>__token__</code> as the username and paste your API token as the password.</li> <li>Or configure Trusted Publishers on PyPI + GitHub.</li> <li>Make sure version, changelog, tests, and docs are updated before publishing.</li> </ul>"},{"location":"project-guide/#how-to-add-a-feature","title":"How to Add a Feature","text":"<p>First, decide where it belongs:</p> If it's a... It goes in... Pure mechanics rule or data model <code>domain/</code> Solver or numerical method <code>engine/</code> User workflow or facade <code>application/</code> CLI command, report, or plot <code>interfaces/</code> <p>Then follow the same pattern every time:</p> <ol> <li>Add the smallest useful implementation in the right layer.</li> <li>Add a test in the matching test folder.</li> <li>Update examples or docs if usage changes.</li> <li>Keep the public API stable when possible.</li> <li>Add a changelog entry if the change is user-visible.</li> </ol> Examples of where things go <ul> <li>New load type \u2192 <code>domain/entities.py</code>, then teach the solver to handle it.</li> <li>New workflow \u2192 <code>application/workflows.py</code> if it's a full analysis step.</li> <li>New CLI command \u2192 <code>interfaces/cli/commands.py</code>.</li> <li>New plot \u2192 <code>interfaces/visual/plots.py</code>.</li> </ul>"},{"location":"project-guide/#how-to-improve-the-library","title":"How to Improve the Library","text":"Engineering improvementsSoftware improvements <ul> <li>Add more load types (triangular distributed, applied couples)</li> <li>Add more support types with clear boundary-condition validation</li> <li>Improve input checking so invalid spans, positions, and units fail early</li> <li>Expand beyond beams: trusses, shafts, frames</li> <li>Add more outputs: deflection, slope, stress envelopes, design checks</li> <li>Keep units explicit and consistent everywhere</li> </ul> <ul> <li>Prefer small classes with one responsibility</li> <li>Keep functions short and testable</li> <li>Put validation close to the data model</li> <li>Use type hints consistently</li> <li>Make interfaces stable and simple for non-software users</li> <li>Add tests for edge cases, not only happy paths</li> <li>Reduce repeated code in beam domain and solver logic</li> <li>Keep plot and CLI layers thin so core calculations stay reusable</li> </ul>"},{"location":"project-guide/#how-to-contribute","title":"How to Contribute","text":"<p>Golden rule</p> <p>Contributions are easiest when they stay local to one layer at a time.</p> <p>Recommended steps:</p> <ol> <li>Create a feature branch.</li> <li>Make one focused change.</li> <li>Run the checks that match your change.</li> <li>Update tests and docs together.</li> <li>Commit with a clear message.</li> <li>Open a pull request describing what changed and why.</li> </ol> <p>Before opening a PR:</p> <pre><code>uv run ruff check src tests examples\nuv run mypy\nuv run pytest\n</code></pre> <p>If the change affects user-facing behavior, also update <code>CHANGELOG.md</code> and the docs.</p>"},{"location":"project-guide/#git-workflow","title":"Git Workflow","text":"<ol> <li>Sync your branch with the latest <code>main</code>.</li> <li>Create a feature branch for one topic only.</li> <li>Keep commits small and descriptive.</li> <li>Run checks before each commit.</li> <li>Push the branch and open a pull request.</li> <li>Merge only after tests, linting, and type checks pass.</li> </ol> <p>Good commit messages</p> <ul> <li><code>Add point moment support to beam analysis</code></li> <li><code>Improve CLI error message for invalid support count</code></li> <li><code>Document solver flow for beam workflows</code></li> </ul>"},{"location":"project-guide/#mental-model","title":"Mental Model","text":"<p>If you're coming from mechanical engineering:</p> Layer Think of it as... <code>domain/</code> The textbook model of the structure <code>engine/</code> The mathematics that solves it <code>application/</code> The lab assistant who collects inputs and runs the procedure <code>interfaces/</code> The way you talk to the program <p>This mental model makes it straightforward to decide where any new feature belongs.</p>"},{"location":"project-guide/#suggested-next-reads","title":"Suggested Next Reads","text":"<ul> <li>Architecture for dependency rules and layering</li> <li>Getting Started for the quickest path to running the library</li> <li>CLI Reference for command-line usage</li> <li>API Reference for auto-generated docs from code ```</li> </ul> <p>What's improved:</p> <ul> <li>Tabbed code blocks for project structure (source vs root) instead of back-to-back walls of text</li> <li>Mermaid diagram for the analysis flow (renders natively with your superfences config)</li> <li>Collapsible sections (<code>??? abstract</code>, <code>??? example</code>) to hide detail until needed</li> <li>Tables instead of bullet lists wherever there's a mapping (layer \u2192 responsibility, class \u2192 purpose)</li> <li>Admonitions (<code>!!! warning</code>, <code>!!! tip</code>, <code>!!! info</code>) for callouts that actually stand out visually</li> <li>Tabbed sections for engineering vs software improvements so readers only see what's relevant to them</li> <li>Tighter copy throughout, no filler paragraphs</li> </ul>"},{"location":"reference/application/","title":"Application","text":"<p>The public-facing facade classes users import directly.</p>"},{"location":"reference/application/#mechlab.application.api","title":"api","text":"<p>Application layer: orchestrates domain + engine into user-friendly workflows. This is the ONE place users should import from.</p>"},{"location":"reference/application/#mechlab.application.api.BeamAnalysis","title":"BeamAnalysis","text":"<pre><code>BeamAnalysis(length: float, material: Material, section: Section)\n</code></pre> <p>High-level facade wrapping Beam construction + solving + reporting, so a user doesn't have to touch domain/engine internals directly.</p> Source code in <code>src/mechlab/application/api.py</code> <pre><code>def __init__(self, length: float, material: Material, section: Section):\n    self.beam = Beam(length, material, section)\n</code></pre>"},{"location":"reference/application/#mechlab.application.api.BeamAnalysis.run","title":"run","text":"<pre><code>run() -&gt; dict\n</code></pre> <p>Solve and return a summary report as a plain dict.</p> Source code in <code>src/mechlab/application/api.py</code> <pre><code>def run(self) -&gt; dict:\n    \"\"\"Solve and return a summary report as a plain dict.\"\"\"\n    self.beam.solve()\n    x_max, sigma_max = self.beam.max_bending_stress()\n    yield_strength = self.beam.material.yield_strength\n    return {\n        \"reactions\": self.beam.reactions(),\n        \"max_bending_stress_Pa\": sigma_max,\n        \"max_bending_stress_location_m\": x_max,\n        \"safety_factor\": yield_strength / sigma_max if sigma_max else float(\"inf\"),\n    }\n</code></pre>"},{"location":"reference/application/#mechlab.application.api.BeamAnalysis.fbd_beam","title":"fbd_beam","text":"<pre><code>fbd_beam(save_path: str | None = None, show: bool = False) -&gt; Any\n</code></pre> <p>Solve the beam if needed and plot the free body diagram.</p> Source code in <code>src/mechlab/application/api.py</code> <pre><code>def fbd_beam(\n    self,\n    save_path: str | None = None,\n    show: bool = False,\n) -&gt; Any:\n    \"\"\"Solve the beam if needed and plot the free body diagram.\"\"\"\n    self.beam.solve()\n    return plot_fbd_beam(self.beam, save_path=save_path, show=show)\n</code></pre>"},{"location":"reference/application/#mechlab.application.api.BeamAnalysis.plot_shear_moment","title":"plot_shear_moment","text":"<pre><code>plot_shear_moment(save_path: str | None = None, show: bool = False) -&gt; Any\n</code></pre> <p>Solve the beam if needed and plot the shear diagrams.</p> Source code in <code>src/mechlab/application/api.py</code> <pre><code>def plot_shear_moment(\n    self,\n    save_path: str | None = None,\n    show: bool = False,\n) -&gt; Any:\n    \"\"\"Solve the beam if needed and plot the shear diagrams.\"\"\"\n    self.beam.solve()\n    return plot_shear(self.beam, save_path=save_path, show=show)\n</code></pre>"},{"location":"reference/application/#mechlab.application.api.BeamAnalysis.plot_bending_moment_diagram","title":"plot_bending_moment_diagram","text":"<pre><code>plot_bending_moment_diagram(save_path: str | None = None, show: bool = False) -&gt; Any\n</code></pre> <p>Solve the beam if needed and plot the bending moment diagrams.</p> Source code in <code>src/mechlab/application/api.py</code> <pre><code>def plot_bending_moment_diagram(\n    self,\n    save_path: str | None = None,\n    show: bool = False,\n) -&gt; Any:\n    \"\"\"Solve the beam if needed and plot the bending moment diagrams.\"\"\"\n    self.beam.solve()\n    return plot_moment(self.beam, save_path=save_path, show=show)\n</code></pre>"},{"location":"reference/application/#mechlab.application.api.BeamAnalysis.add_moment","title":"add_moment","text":"<pre><code>add_moment(position: float, magnitude: float) -&gt; BeamAnalysis\n</code></pre> <p>Add a concentrated applied moment (couple) at a point.</p> <p>Sign convention: positive = counter-clockwise (N\u00b7m).</p> Source code in <code>src/mechlab/application/api.py</code> <pre><code>def add_moment(self, position: float, magnitude: float) -&gt; BeamAnalysis:\n    \"\"\"\n    Add a concentrated applied moment (couple) at a point.\n\n    Sign convention: positive = counter-clockwise (N\u00b7m).\n    \"\"\"\n    self.beam.add_load(PointMoment(position, magnitude))\n    return self\n</code></pre>"},{"location":"reference/domain/","title":"Domain","text":"<p>Pure engineering science \u2014 no I/O, no external dependencies.</p>"},{"location":"reference/domain/#mechlab.domain.entities","title":"entities","text":"<p>Core domain entities \u2014 pure data + behavior, no I/O, no external deps. This is the OOP backbone: everything downstream (statics, dynamics, strength) builds on these classes.</p>"},{"location":"reference/domain/#mechlab.domain.entities.Material","title":"Material  <code>dataclass</code>","text":"<pre><code>Material(name: str, young_modulus: float, yield_strength: float)\n</code></pre> <p>Immutable material properties.</p>"},{"location":"reference/domain/#mechlab.domain.entities.Section","title":"Section  <code>dataclass</code>","text":"<pre><code>Section(name: str, moment_of_inertia: float, area: float, extreme_fiber_distance: float)\n</code></pre> <p>Cross-sectional geometric properties.</p>"},{"location":"reference/domain/#mechlab.domain.entities.Section.section_modulus","title":"section_modulus","text":"<pre><code>section_modulus() -&gt; float\n</code></pre> <p>Z = I / c \u2014 used for bending stress calcs.</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>def section_modulus(self) -&gt; float:\n    \"\"\"Z = I / c \u2014 used for bending stress calcs.\"\"\"\n    return self.moment_of_inertia / self.extreme_fiber_distance\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.Load","title":"Load","text":"<pre><code>Load(position: float)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base for any load acting on a Body.</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>def __init__(self, position: float):\n    self.position = position  # location along the body, in m\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.Load.total_force","title":"total_force  <code>abstractmethod</code>","text":"<pre><code>total_force() -&gt; float\n</code></pre> <p>Resultant force magnitude (N), downward positive.</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>@abstractmethod\ndef total_force(self) -&gt; float:\n    \"\"\"Resultant force magnitude (N), downward positive.\"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.Load.moment_about","title":"moment_about  <code>abstractmethod</code>","text":"<pre><code>moment_about(ref_position: float) -&gt; float\n</code></pre> <p>Moment this load creates about a reference point (N\u00b7m).</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>@abstractmethod\ndef moment_about(self, ref_position: float) -&gt; float:\n    \"\"\"Moment this load creates about a reference point (N\u00b7m).\"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.DistributedLoad","title":"DistributedLoad","text":"<pre><code>DistributedLoad(start: float, end: float, intensity: float)\n</code></pre> <p>               Bases: <code>Load</code></p> <p>Uniformly distributed load over [start, end].</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>def __init__(self, start: float, end: float, intensity: float):\n    if end &lt;= start:\n        raise ValueError(\"end must be greater than start\")\n    super().__init__(position=(start + end) / 2)  # centroid\n    self.start = start\n    self.end = end\n    self.intensity = intensity  # N/m\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.PointMoment","title":"PointMoment","text":"<pre><code>PointMoment(position: float, magnitude: float)\n</code></pre> <p>               Bases: <code>Load</code></p> <p>A concentrated applied moment (couple) acting at a point.</p> <p>A pure couple has zero net force \u2014 it doesn't move the beam, only twists it \u2014 so total_force() is always 0. Its moment about any reference point is the couple's own magnitude, since a couple's moment is independent of the point you take it about.</p> <p>Sign convention: positive = counter-clockwise, in N\u00b7m.</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>def __init__(self, position: float, magnitude: float):\n    super().__init__(position)\n    self.magnitude = magnitude  # N\u00b7m, CCW positive\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.Body","title":"Body","text":"<pre><code>Body(length: float, material: Material, section: Section)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base for anything analyzable: Beam, Truss, Shaft, etc. Subclasses must implement equilibrium-solving behavior.</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>def __init__(self, length: float, material: Material, section: Section):\n    self.length = length\n    self.material = material\n    self.section = section\n    self.loads: list[Load] = []\n    self.supports: list[Support] = []\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.Body.solve","title":"solve  <code>abstractmethod</code>","text":"<pre><code>solve() -&gt; None\n</code></pre> <p>Solve equilibrium / internal reactions. Implemented by subclass.</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>@abstractmethod\ndef solve(self) -&gt; None:\n    \"\"\"Solve equilibrium / internal reactions. Implemented by subclass.\"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.entities.SectionProperties","title":"SectionProperties  <code>dataclass</code>","text":"<pre><code>SectionProperties(area: float, centroid: tuple[float, float], Ixx: float, Iyy: float, Ixy: float, J: float, I_max: float, I_min: float, principal_angle_deg: float, rx: float, ry: float, c_top: float, c_bottom: float, c_left: float, c_right: float, Sx_top: float, Sx_bottom: float, Sy_left: float, Sy_right: float, perimeter: float, vertices: ndarray)\n</code></pre> <p>All standard geometric properties of a 2D cross-section.</p>"},{"location":"reference/domain/#mechlab.domain.entities.compute_properties","title":"compute_properties","text":"<pre><code>compute_properties(shapes: list[ndarray]) -&gt; SectionProperties\n</code></pre> <p>Compute full section properties for a shape made of one or more closed polygons.</p> <p><code>shapes</code> is a list of (N, 2) vertex arrays. Give solid regions in counter-clockwise order and holes in clockwise order (their signed area comes out negative automatically and is subtracted for you). A single solid shape is just <code>shapes=[vertices]</code>.</p> Source code in <code>src/mechlab/domain/entities.py</code> <pre><code>def compute_properties(shapes: list[np.ndarray]) -&gt; SectionProperties:\n    \"\"\"\n    Compute full section properties for a shape made of one or more\n    closed polygons.\n\n    `shapes` is a list of (N, 2) vertex arrays. Give solid regions in\n    counter-clockwise order and holes in clockwise order (their signed\n    area comes out negative automatically and is subtracted for you).\n    A single solid shape is just `shapes=[vertices]`.\n    \"\"\"\n    A_total = 0.0\n    Qx_total = 0.0\n    Qy_total = 0.0\n    Ixx_o_total = 0.0\n    Iyy_o_total = 0.0\n    Ixy_o_total = 0.0\n    perimeter_total = 0.0\n\n    for verts in shapes:\n        verts = np.asarray(verts, dtype=float)\n        A, Qx, Qy, Ixx_o, Iyy_o, Ixy_o = _raw_moments(verts)\n        A_total += A\n        Qx_total += Qx\n        Qy_total += Qy\n        Ixx_o_total += Ixx_o\n        Iyy_o_total += Iyy_o\n        Ixy_o_total += Ixy_o\n        perimeter_total += _perimeter(verts)\n\n    if A_total &lt;= 0:\n        raise ValueError(\n            \"Total area came out zero or negative. Make sure solid outlines are \"\n            \"counter-clockwise and holes are clockwise.\"\n        )\n\n    cx = Qy_total / A_total\n    cy = Qx_total / A_total\n\n    # Parallel axis theorem: shift from origin to the combined centroid\n    Ixx = Ixx_o_total - A_total * cy**2\n    Iyy = Iyy_o_total - A_total * cx**2\n    Ixy = Ixy_o_total - A_total * cx * cy\n    J = Ixx + Iyy\n\n    # Principal moments of inertia (Mohr's circle for inertia)\n    I_avg = (Ixx + Iyy) / 2.0\n    R = np.sqrt(((Ixx - Iyy) / 2.0) ** 2 + Ixy**2)\n    I_max = I_avg + R\n    I_min = I_avg - R\n    theta_p = 0.5 * np.degrees(np.arctan2(-2 * Ixy, Ixx - Iyy))\n\n    rx = np.sqrt(Ixx / A_total)\n    ry = np.sqrt(Iyy / A_total)\n\n    all_x = np.concatenate([np.asarray(s)[:, 0] for s in shapes])\n    all_y = np.concatenate([np.asarray(s)[:, 1] for s in shapes])\n\n    c_top = float(np.max(all_y) - cy)\n    c_bottom = float(cy - np.min(all_y))\n    c_right = float(np.max(all_x) - cx)\n    c_left = float(cx - np.min(all_x))\n\n    def safe_div(a: float, b: float) -&gt; float:\n        return float(a / b) if b &gt; 1e-15 else float(\"inf\")\n\n    return SectionProperties(\n        area=A_total,\n        centroid=(cx, cy),\n        Ixx=Ixx,\n        Iyy=Iyy,\n        Ixy=Ixy,\n        J=J,\n        I_max=I_max,\n        I_min=I_min,\n        principal_angle_deg=theta_p,\n        rx=rx,\n        ry=ry,\n        c_top=c_top,\n        c_bottom=c_bottom,\n        c_left=c_left,\n        c_right=c_right,\n        Sx_top=safe_div(Ixx, c_top),\n        Sx_bottom=safe_div(Ixx, c_bottom),\n        Sy_left=safe_div(Iyy, c_left),\n        Sy_right=safe_div(Iyy, c_right),\n        perimeter=perimeter_total,\n        vertices=np.array(shapes, dtype=object),\n    )\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.strength.beam","title":"beam","text":"<p>Beam: a concrete domain entity that inherits from Body. Demonstrates OOP inheritance + delegation to an engine-layer solver (composition over inheritance for the solving algorithm itself).</p>"},{"location":"reference/domain/#mechlab.domain.strength.beam.Beam","title":"Beam","text":"<pre><code>Beam(length: float, material: Material, section: Section)\n</code></pre> <p>               Bases: <code>Body</code></p> <p>A simply-supported beam under point/distributed vertical loads.</p> Source code in <code>src/mechlab/domain/strength/beam.py</code> <pre><code>def __init__(self, length: float, material: Material, section: Section):\n    super().__init__(length, material, section)\n    self._solver = MatrixBeamSolver()\n    self._solved = False\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.strength.beam.Beam.solve","title":"solve","text":"<pre><code>solve() -&gt; None\n</code></pre> <p>Compute support reactions using the matrix beam solver.</p> Source code in <code>src/mechlab/domain/strength/beam.py</code> <pre><code>def solve(self) -&gt; None:\n    \"\"\"Compute support reactions using the matrix beam solver.\"\"\"\n    self._solver.solve(\n        self.length, self.loads, self.supports\n    )\n    self._solved = True\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.strength.beam.Beam.shear_at","title":"shear_at","text":"<pre><code>shear_at(x: float) -&gt; float\n</code></pre> <p>Internal shear force at position x (from the left end).</p> Source code in <code>src/mechlab/domain/strength/beam.py</code> <pre><code>def shear_at(self, x: float) -&gt; float:\n    \"\"\"Internal shear force at position x (from the left end).\"\"\"\n    self._require_solved()\n    v = 0.0\n    for s in self.supports:\n        if s.position &lt;= x:\n            v += s.reaction_force\n    for load in self.loads:\n        if load.position &lt;= x:\n            v -= load.total_force()\n    return v\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.strength.beam.Beam.moment_at","title":"moment_at","text":"<pre><code>moment_at(x: float) -&gt; float\n</code></pre> <p>Internal bending moment at position x (from the left end).</p> Source code in <code>src/mechlab/domain/strength/beam.py</code> <pre><code>def moment_at(self, x: float) -&gt; float:\n    \"\"\"Internal bending moment at position x (from the left end).\"\"\"\n    self._require_solved()\n    m = 0.0\n    for s in self.supports:\n        if s.position &lt;= x:\n            m += s.reaction_force * (x - s.position)\n    for load in self.loads:\n        if load.position &lt;= x:\n            m -= load.total_force() * (x - load.position)\n    return m\n</code></pre>"},{"location":"reference/domain/#mechlab.domain.strength.beam.Beam.max_bending_stress","title":"max_bending_stress","text":"<pre><code>max_bending_stress(num_points: int = 200) -&gt; tuple[float, float]\n</code></pre> <p>Returns (position, stress) of maximum absolute bending stress.</p> Source code in <code>src/mechlab/domain/strength/beam.py</code> <pre><code>def max_bending_stress(self, num_points: int = 200) -&gt; tuple[float, float]:\n    \"\"\"Returns (position, stress) of maximum absolute bending stress.\"\"\"\n    self._require_solved()\n    z = self.section.section_modulus()\n    best_x, best_stress = 0.0, 0.0\n    for i in range(num_points + 1):\n        x = self.length * i / num_points\n        stress = abs(self.moment_at(x)) / z\n        if stress &gt; best_stress:\n            best_x, best_stress = x, stress\n    return best_x, best_stress\n</code></pre>"},{"location":"reference/engine/","title":"Engine","text":"<p>Numerical machinery: solvers and unit conversion.</p>"},{"location":"reference/engine/#mechlab.engine.math.solvers","title":"solvers","text":"<p>Numerical/computation layer. Solvers know nothing about \"beams\" \u2014 they operate on generic loads/supports handed to them. This keeps domain classes decoupled from solving algorithms (Strategy pattern).</p>"},{"location":"reference/engine/#mechlab.engine.math.solvers.EquilibriumSolver","title":"EquilibriumSolver","text":"<p>Solves reactions for a simply-supported system with exactly two vertical supports (pin/roller) under vertical loads only.</p>"},{"location":"reference/engine/#mechlab.engine.math.solvers.MatrixBeamSolver","title":"MatrixBeamSolver","text":"<p>A general beam solver capable of solving statically determinate and indeterminate beams with any number of vertical supports using the Matrix Stiffness Method.</p>"},{"location":"reference/engine/#mechlab.engine.units.registry","title":"registry","text":"<p>engine.units.registry \u2014 a minimal unit-conversion system.</p> <p>Internally, mechlab always works in SI base units (meters, newtons, pascals, kilograms, seconds). This module exists so input/output can be given in other common engineering units without polluting the domain layer with unit-awareness.</p> Example <pre><code>&gt;&gt;&gt; from mechlab.engine.units.registry import UnitRegistry\n&gt;&gt;&gt; ur = UnitRegistry()\n&gt;&gt;&gt; ur.to_si(50, \"kN\")\n50000.0\n&gt;&gt;&gt; ur.from_si(50000, \"kN\")\n50.0\n</code></pre>"},{"location":"reference/engine/#mechlab.engine.units.registry.UnitRegistry","title":"UnitRegistry","text":"<pre><code>UnitRegistry()\n</code></pre> <p>Holds conversion factors to/from SI base units.</p> <p>Each entry maps a unit symbol -&gt; multiplier such that:     si_value = input_value * factor     input_value = si_value / factor</p> <p>Attributes:</p> Name Type Description <code>_factors</code> <code>dict[str, float]</code> <p>internal mapping of unit symbol to SI multiplier.</p> Source code in <code>src/mechlab/engine/units/registry.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._factors: dict[str, float] = {\n        # length (-&gt; meters)\n        \"m\": 1.0,\n        \"mm\": 1e-3,\n        \"cm\": 1e-2,\n        \"in\": 0.0254,\n        \"ft\": 0.3048,\n        # force (-&gt; newtons)\n        \"N\": 1.0,\n        \"kN\": 1e3,\n        \"lbf\": 4.4482216153,\n        # pressure / stress (-&gt; pascals)\n        \"Pa\": 1.0,\n        \"kPa\": 1e3,\n        \"MPa\": 1e6,\n        \"GPa\": 1e9,\n        \"psi\": 6894.757293168,\n    }\n</code></pre>"},{"location":"reference/engine/#mechlab.engine.units.registry.UnitRegistry.register","title":"register","text":"<pre><code>register(symbol: str, si_factor: float) -&gt; None\n</code></pre> <p>Add or override a unit's SI conversion factor.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>unit symbol, e.g. \"kip\".</p> required <code>si_factor</code> <code>float</code> <p>multiplier such that value_in_symbol * si_factor = value_in_SI.</p> required Source code in <code>src/mechlab/engine/units/registry.py</code> <pre><code>def register(self, symbol: str, si_factor: float) -&gt; None:\n    \"\"\"Add or override a unit's SI conversion factor.\n\n    Args:\n        symbol: unit symbol, e.g. \"kip\".\n        si_factor: multiplier such that value_in_symbol * si_factor = value_in_SI.\n    \"\"\"\n    self._factors[symbol] = si_factor\n</code></pre>"},{"location":"reference/engine/#mechlab.engine.units.registry.UnitRegistry.to_si","title":"to_si","text":"<pre><code>to_si(value: float, unit: str) -&gt; float\n</code></pre> <p>Convert a value FROM the given unit TO SI base units.</p> Source code in <code>src/mechlab/engine/units/registry.py</code> <pre><code>def to_si(self, value: float, unit: str) -&gt; float:\n    \"\"\"Convert a value FROM the given unit TO SI base units.\"\"\"\n    self._assert_known(unit)\n    return value * self._factors[unit]\n</code></pre>"},{"location":"reference/engine/#mechlab.engine.units.registry.UnitRegistry.from_si","title":"from_si","text":"<pre><code>from_si(si_value: float, unit: str) -&gt; float\n</code></pre> <p>Convert a value FROM SI base units TO the given unit.</p> Source code in <code>src/mechlab/engine/units/registry.py</code> <pre><code>def from_si(self, si_value: float, unit: str) -&gt; float:\n    \"\"\"Convert a value FROM SI base units TO the given unit.\"\"\"\n    self._assert_known(unit)\n    return si_value / self._factors[unit]\n</code></pre>"},{"location":"reference/interfaces/","title":"Interfaces","text":"<p>CLI commands and report generation.</p>"},{"location":"reference/interfaces/#mechlab.interfaces.cli.commands","title":"commands","text":"<p>interfaces.cli.commands \u2014 command-line entry point.</p> Usage <p>python -m mechlab beam --length 4.0 --E 200e9 --yield 250e6 \\     --I 9.19e-6 --area 2.3e-3 --c 0.076 \\     --support 0.0 --support 4.0 \\     --point-load 2.0 5000</p> <p>This module only parses args and calls into <code>application/</code> \u2014 it must never construct domain objects (Beam, Material, ...) directly from raw CLI strings without going through the application facade, so behavior stays identical whether triggered by CLI, script, or API.</p>"},{"location":"reference/interfaces/#mechlab.interfaces.cli.commands.run_beam_command","title":"run_beam_command","text":"<pre><code>run_beam_command(args: Namespace) -&gt; str\n</code></pre> <p>Executes the 'beam' subcommand and returns a formatted report string.</p> Source code in <code>src/mechlab/interfaces/cli/commands.py</code> <pre><code>def run_beam_command(args: argparse.Namespace) -&gt; str:\n    \"\"\"Executes the 'beam' subcommand and returns a formatted report string.\"\"\"\n    if len(args.support) != 2:\n        raise SystemExit(\"Exactly two --support values are required\")\n\n    material = Material(name=\"user-material\", young_modulus=args.E,\n                         yield_strength=args.yield_strength)\n    section = Section(name=\"user-section\", moment_of_inertia=args.I,\n                       area=args.area, extreme_fiber_distance=args.c)\n\n    analysis = BeamAnalysis(length=args.length, material=material, section=section)\n    analysis.set_simple_supports(args.support[0], args.support[1])\n    for position, magnitude in args.point_load:\n        analysis.add_point_load(position, magnitude)\n\n    result = analysis.run()\n    return ReportGenerator().to_text(result)\n</code></pre>"},{"location":"reference/interfaces/#mechlab.interfaces.output.report","title":"report","text":"<p>interfaces.output.report \u2014 formats analysis results for humans.</p> <p>This module ONLY does presentation. It must never contain physics or solving logic \u2014 if you find yourself computing a new value here instead of formatting an existing one, that logic belongs in domain/ or application/ instead.</p>"},{"location":"reference/interfaces/#mechlab.interfaces.output.report.ReportGenerator","title":"ReportGenerator","text":"<p>Formats a BeamAnalysis.run() result dict into readable output.</p>"},{"location":"reference/interfaces/#mechlab.interfaces.output.report.ReportGenerator.to_text","title":"to_text","text":"<pre><code>to_text(report: dict[str, Any]) -&gt; str\n</code></pre> <p>Render the report as a plain-text block.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>dict[str, Any]</code> <p>the dict returned by BeamAnalysis.run().</p> required <p>Returns:</p> Type Description <code>str</code> <p>A multi-line human-readable string.</p> Source code in <code>src/mechlab/interfaces/output/report.py</code> <pre><code>def to_text(self, report: dict[str, Any]) -&gt; str:\n    \"\"\"Render the report as a plain-text block.\n\n    Args:\n        report: the dict returned by BeamAnalysis.run().\n\n    Returns:\n        A multi-line human-readable string.\n    \"\"\"\n    lines = [\"=== Beam Analysis Report ===\", \"\"]\n\n    lines.append(\"Reactions:\")\n    for pos, force in report[\"reactions\"]:\n        lines.append(f\"  x = {pos:.3f} m  -&gt;  R = {force:.2f} N\")\n\n    lines.append(\"\")\n    stress_mpa = report[\"max_bending_stress_Pa\"] / 1e6\n    lines.append(f\"Max bending stress : {stress_mpa:.2f} MPa \"\n                  f\"at x = {report['max_bending_stress_location_m']:.2f} m\")\n    lines.append(f\"Safety factor       : {report['safety_factor']:.2f}\")\n\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/interfaces/#mechlab.interfaces.output.report.ReportGenerator.to_dict","title":"to_dict","text":"<pre><code>to_dict(report: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> <p>Pass-through for JSON/API consumers \u2014 kept for symmetry with to_text.</p> Source code in <code>src/mechlab/interfaces/output/report.py</code> <pre><code>def to_dict(self, report: dict[str, Any]) -&gt; dict[str, Any]:\n    \"\"\"Pass-through for JSON/API consumers \u2014 kept for symmetry with to_text.\"\"\"\n    return report\n</code></pre>"},{"location":"tutorial/MachineDesign/","title":"Machine Design","text":"<p>How to design a machine learning model for your specific problem.</p>"}]}